version: "3.8"

services:
  board-service:
    container_name: payment-service
    image: ${DOCKER_USERNAME}/payment-service:latest
    ports:
      - "8200:8200"
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    external: true